import React from 'react'
import ReactDOM from 'react-dom/client'
import LakeviewUniversity from './LakeviewUniversity.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LakeviewUniversity />
  </React.StrictMode>,
)
